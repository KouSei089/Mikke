<% @diaries.each do |diary| %>
<div class="p-8">
  <div class="bg-white rounded-lg shadow-xl">
    <div class="p-16">
      <div>
       <!-- Insert your svg or image here -->
      </div>
      <div class="mt-8 text-center">
        <h1 class="font-bold text-lg text-gray-700 mb-1"><%= diary.title %></h1>
        <p class="text-gray-600"><%= diary.text %></p>
        <button class="mt-6 bg-blue-500 hover:bg-blue-400 text-white rounded-full px-12 py-3 shadow-xl focus:outline-none">
          Get started
        </button>
        <button class="mt-6 bg-red-500 hover:bg-red-400 text-white rounded-full px-12 py-3 shadow-xl focus:outline-none">
          Get started
        </button>
      </div>
    </div>
  </div>
</div>
<% end %>
