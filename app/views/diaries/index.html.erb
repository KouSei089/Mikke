<% @diaries.each do |diary| %>
  <div class="p-8">
    <div class="bg-gray-50 rounded-3xl shadow-lg">
      <div class="p-16">
        <div>
          <h1 class="font-bold text-lg text-gray-700 mb-1">月</h1>
          <h1 class="font-bold text-lg text-gray-700 mb-1 text-center"><%= diary.title %></h1>
        </div>
        <div class="mt-8 text-center">
          <p class="text-gray-600"><%= diary.text %></p>
          <button class="mt-6 bg-indigo-300 hover:bg-indigo-200 text-gray-700 rounded-full px-12 py-3 shadow-lg focus:outline-none">
            Get started
          </button>
          <button class="mt-6 bg-pink-200 hover:bg-pink-100 text-gray-700 rounded-full px-12 py-3 shadow-lg focus:outline-none">
            Get started
          </button>
        </div>
      </div>
    </div>
  </div>
<% end %>
